import {
  noa::gen {Gen, apply, gen},
  noa::gen.list {gen-list},
  noa::suite {gen-kit},
  this::field.gen {gen-field},
  this::header {header},
}

constant gen-header: gen(header) {
  Gen(
    @<r>(k: &gen-kit) => {
      apply@<r, header>(gen-list(gen-field), k)
    },
  )
}
