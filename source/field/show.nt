import {
  core.show {Show, show},
  core.text {join},
  this.field {Field, field},
}

define show-field(h: field): text {
  let Field{key, value} = h;
  join(List[key, *": ", value])
}

inline as-show: show(field) {
  Show{show := show-field}
}
